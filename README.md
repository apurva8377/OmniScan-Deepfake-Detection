# OmniScan-Deepfake-Detection
A real-time deepfake detection system built with PyTorch and Django. Uses a Chrome extension to analyze media streams via Spatial (CNN), Temporal (LSTM), and Auditory neural networks.
